version: '3.8'

services:
  job-queue:
    build: .
    container_name: reliable-job-queue
    ports:
      - "3000:3000"
    volumes:
      - queue-data:/app/data
    restart: unless-stopped
    environment:
      - PORT=3000
      - NODE_ENV=production

volumes:
  queue-data:
    driver: local
